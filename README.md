# CordisX Documentation Site

Documentation aggregation and publishing for `https://cordisx.github.io/docs/`.

This repository does not own product documentation. It pulls `.agents/docs` from source repositories declared in `sources.yaml`.
