{
  "name": "@cordisx/docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check preferences.js && node --check site-links.js && node --check reicons.js && node scripts/check.mjs"
  },
  "engines": {
    "node": ">=22.19"
  }
}
